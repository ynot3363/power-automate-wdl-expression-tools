if(
    equals(
        toLower(
            trim(
                coalesce(
                    outputs('Get_User')?['body/Email'],
                    ''
                )
            )
        ),
        'test@contoso.com'
    ),
    concat(
        'Hello ',
        outputs('Get_User')?['body/DisplayName']
    ),
    'Unknown'
)
